N = int(input())
arr = [list(map(int, input().split())) for _ in range(N)]

arr.sort(key=lambda x: x[1])

cnt = 0
now_time = 0

for start, end in arr:
    if start >= now_time:
        cnt += 1
        now_time = end+1

print(cnt)
